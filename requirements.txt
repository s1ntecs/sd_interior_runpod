# CORE ML LIBRARIES (CUDA 11.8)
torch==2.2.1             # PyTorch для CUDA 11.8
torchvision==0.15.2
torchaudio==2.2.1

# DIFFUSERS + PEFT/LoRA SUPPORT
diffusers>=0.30.0        # поддержка StableDiffusionPipeline
transformers>=4.34.0     # содержит CLIPImageProcessor
peft>=0.6.0              # PEFT backend ≥0.6.0
accelerate>=0.27,<0.28   # оптимальный билд для infer/training
safetensors              # быстрый безопасный формат весов

# MODEL UTILITIES & CONFIG
einops                   # перестановка тензоров
omegaconf                # управление YAML-конфигами
huggingface-hub          # загрузка моделей из HF-репозиториев

# IMAGE PROCESSING & DETECTION
opencv-python            # OpenCV для I/O изображений
Pillow                   # PIL для работы с изображениями
numpy                    # базовые численные операции
mediapipe                # опционально: детекция поз/рук

# NETWORK & RUNTIME
aiohttp                  # асинхронные HTTP-запросы
pyyaml                   # YAML-парсер
importlib_metadata       # для Python <3.10

# OPTIONAL RUNPOD SDK
runpod                   # интеграция с RunPod serverless
xformers==0.0.22         # ускорение attention (если нужно)
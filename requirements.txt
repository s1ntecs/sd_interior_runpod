# Diffusers с поддержкой PEFT/LoRA
diffusers>=0.30.0              # полная поддержка интеграции с PEFT
peft>=0.6.0                    # PEFT backend ≥0.6.0 для использования use_peft_backend=True
transformers>=4.34.0           # совместимая версия Transformers
accelerate>=0.27,<0.28         # ускорение infer и training pipelines
safetensors                    # быстрый и безопасный формат весов

# Утилиты для работы с моделями и конфигами
einops                         # перестановка тензоров
omegaconf                      # YAML-конфиги
huggingface-hub                # загрузка моделей из HF Hub

# Библиотеки для предварительной обработки изображений и детекции
opencv-python                  # OpenCV для чтения/записи изображений
Pillow                         # работа с изображениями в PIL
numpy                          # базовые числовые операции
mediapipe                      # если нужен детектор поз/рук

# Сетевая и вспомогательная инфраструктура
aiohttp                        # асинхронные HTTP-запросы
pyyaml                         # парсинг YAML
importlib_metadata             # для Python <3.10

# RunPod SDK (если используется)
runpod                         # клиент RunPod для serverless